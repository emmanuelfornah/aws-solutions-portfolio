# Python dependencies for Flask application on Elastic Beanstalk
# These packages will be installed automatically during EB deployment

# Flask web framework - lightweight WSGI web application framework
Flask==2.3.3

# Werkzeug - WSGI utility library (Flask dependency)
Werkzeug==2.3.7

# Click - command line interface creation kit (Flask dependency)
click==8.1.7

# Jinja2 - templating engine (Flask dependency)
Jinja2==3.1.2

# MarkupSafe - string handling library (Jinja2 dependency)
MarkupSafe==2.1.3

# itsdangerous - data signing library (Flask dependency)
itsdangerous==2.1.2
